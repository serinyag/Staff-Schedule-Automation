# Agents

This layer is reserved for optional specialized agents such as:

- coverage planner
- fairness or pattern optimizer
- manager-review explainer

Agents must not write to Supabase or approve exceptions. They are advisory components inside a deterministic rule envelope.
