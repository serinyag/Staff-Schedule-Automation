# Rules Layer

This layer will contain the shared implementation of hard, soft, and exception rules.

Planner and validator must use the same conceptual rules, even when they apply them differently.

Rule definitions belong in Git. Person-specific values belong in Supabase.
