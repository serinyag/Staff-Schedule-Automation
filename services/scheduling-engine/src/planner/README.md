# Planner

This layer will produce draft schedules and uncovered-shift results.

Responsibilities:

- build draft assignment proposals
- return uncovered shifts explicitly
- optimize within rule and budget boundaries
- never publish directly
