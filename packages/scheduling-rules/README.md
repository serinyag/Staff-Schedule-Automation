# Scheduling Rules

Stable rule definitions live in Git.

Person-specific values live in Supabase.

Examples:

- Git defines what a weekly maximum means.
- Supabase defines each staff member's actual minimum, target, and maximum.

Rule definitions and operational parameters must not be confused.

Every generated run should record:

- `rules_version`
- `engine_version`
